/** Descriptor-only Evotown plugin — runtime wiring via openclaw.plugin.json setup.providers */
export default function register(_api) {
  // No runtime hooks required; OpenClaw reads setup.providers envVars from the manifest.
}
